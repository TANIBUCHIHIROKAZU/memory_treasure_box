<h3>お問い合わせ一覧</h3>
<% @contact.each do |contact| %>
 <div>
  <%= link_to admin_contact_path(contact) do %>
    <%= contact.created_at.strftime('%Y/%m/%d') %>
    <%= contact.name %>
    <%= truncate(contact.subject,length: 20)  %>
  <% end %>
<% end %> 
<%= paginate @contact %>